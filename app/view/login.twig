<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
		<link rel="stylesheet" href="./resource/style.css">
		<title>Login</title>
	</head>
	<body>
		<h1>Scuba<span>PHP</span>
		</h1>

		{% if success_message %}
			<div class="mensagem-sucesso">{{ success_message }}</div>
		{% endif %}

		{% if error_message %}
			<div class="mensagem-erro">{{ error_message }}</div>
		{% endif %}

		<form action="/login/check" method="POST">
			<label for="login">Email</label>
			<input type="text" name="person[email]" required>
			<span class="mensagem-erro">Mensagem de Erro</span>

			<label for="password">Senha</label>
			<input type="password" name="person[password]" required>
			<span class="mensagem-erro">Mensagem de Erro</span>

			<button>Entrar</button>
			<a href="/register">Cadastrar Usuário</a>
			<a href="./?page=forget-password">Esqueci Minha Senha</a>
		</form>
	</body>
</html>
