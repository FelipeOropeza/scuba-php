<!DOCTYPE html>
<html lang="pt-br">
	<head>
		<meta charset="UTF-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
		<link rel="stylesheet" href="./resource/style.css">
		<title>Registre</title>
	</head>
	<body>
		<a href="/">
			<h1>Scuba<span>PHP</span>
			</h1>
		</a>
		<form action="/register/create" method="POST">
			<p>Cadastre Um Novo Usuário</p>
			<label for="nome">Nome</label>
			<input type="text" id="name" name="person[name]" value="{{ person.name ?? '' }}">
			<span class="mensagem-erro">{{ errors['name'] }}</span>
			<label for="email">E-mail</label>
			<input type="email" id="email" name="person[email]" value="{{ person.email ?? '' }}">
			<span class="mensagem-erro">{{ errors['email'] }}</span>
			<label for="senha">Senha</label>
			<input type="password" id="password" name="person[password]" value="{{ person.password ?? '' }}">
			<span class="mensagem-erro">{{ errors['password'] }}</span>
			<label for="repita-senha">Repita Senha</label>
			<input type="password" name="person[password-confirm]">
			<span class="mensagem-erro">{{ errors['password-confirm'] }}</span>
			<input type="submit" value="Salvar">
		</form>
	</body>
</html>
